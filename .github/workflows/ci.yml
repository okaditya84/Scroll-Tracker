name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-verify:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # undici@7+ and some other deps require Node >=20.18.1; pin the runner to a compatible Node
        # to avoid runtime ReferenceError: File is not defined and engine mismatch warnings.
        node-version: ['20.18.1']
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Check Node/NPM versions
        run: |
          node -v
          npm -v

      - name: Build backend (typescript)
        working-directory: ./backend
        run: |
          npm ci
          npm run build

      - name: Build web (next) (light)
        working-directory: ./web
        run: |
          npm ci
          npm run build || true

      - name: Build extension (chrome)
        working-directory: ./extension
        run: |
          npm ci
          npm run build

      - name: Build extension-edge (edge)
        working-directory: ./extension-edge
        run: |
          npm ci
          npm run build

      - name: Run manifest verification
        run: node ./tools/verifyManifests.js

      - name: Archive extension artifacts
        uses: actions/upload-artifact@v4
        with:
          name: extension-artifacts
          path: |
            extension/dist
            extension-edge/dist
