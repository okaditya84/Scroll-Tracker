NODE_ENV=development
PORT=3000
MONGODB_URI=
JWT_SECRET=
JWT_EXPIRES_IN=15m
JWT_REFRESH_TTL_DAYS=30
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GROQ_API_KEY=
FRONTEND_URL=http://localhost:3001
EXTENSION_URL=chrome-extension://your-extension-id
# Optional: comma-separated list if you have multiple extension builds (e.g. dev + production)
EXTENSION_URLS=
# Optional: add fully-qualified extra origins (comma-separated) such as preview deploys
CORS_ADDITIONAL_ORIGINS=

# Comma-separated list of admin emails that should automatically receive the admin role on login
ADMIN_EMAILS=

# SMTP / Email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=tryreverseai@gmail.com
SMTP_PASSWORD=
SMTP_FROM_EMAIL=tryreverseai@gmail.com
SMTP_FROM_NAME=Scrollwise

# OTP configuration
OTP_TTL_MINUTES=10
OTP_THROTTLE_PER_HOUR=5
OTP_MAX_ATTEMPTS=5
